# Current Reality Snapshot

- Snapshot date: 2026-08-14.
- Repository root is a Git worktree on `main` and contains `.git/`, `agent_pack/`, and `docs/`.
- The worktree entered `backend_014` with the authorized Q-002 product-decision and generated task-state changes already present; those changes were preserved while the selected task added only its scoped runtime, contracts, tests, API artifacts, documentation, and Agent Pack synchronization.
- The npm workspace foundation is present: root `package.json`, `package-lock.json`, `tsconfig.base.json`, `.gitignore`, and manifests for `apps/api`, `apps/web`, `packages/contracts`, `packages/ui`, and `packages/config`.
- Structural policy scripts and tests are present under `packages/config/`; the repository now has ESLint source checks, disjoint unit/integration/API test harnesses, enforced native coverage thresholds, a local `quality` gate, and a basic Node 24 GitHub Actions workflow.
- The API exposes operational `/health` and `/ready`, shared Admin/password and phone-OTP authentication/session routes, Seeker registration/profile/preferences, seven Provider onboarding routes, four private Provider-document routes, three permission-enforced Admin role routes, two reason-bearing account/Provider-review transition routes, and two permission-gated Admin audit-log read routes. Provider onboarding creates the verified-phone identity from a single-use OTP grant, supports owner-scoped optimistic draft edits, pins versioned requirements, validates submission against the clean private-document inventory, and freezes the submission snapshot. Provider-document runtime implements generated-key streaming, PDF/JPEG/PNG signature checks, 10 MiB limits, private quarantine, deterministic Local/Test scanning, fail-closed higher environments, independent security/review states, idempotent versioned replacement, retention metadata, and authorized 300-second delivery without public URLs. Admin login uses normalized email plus Argon2id and the common rotating-session model; the first Super Admin has an explicit internal one-time transactional bootstrap command with strict input, an immutable unique guard, and no HTTP bootstrap or checked-in credentials. Dynamic RBAC uses a closed stable capability catalog, explicit custom/View Only modes, current-account checks, bootstrap-only full access, active-role permission unions, optimistic role changes, bounded versioned Admin assignments, and a reusable fail-closed object policy. Account moderation enforces the approved state machines, atomic User/Profile/Application Provider review, mandatory reasons, immutable transition evidence, optimistic conflict detection, active-session revocation, and current-session/status checks before protected product routers; Admin lifecycle remains dependency-ordered under `backend_121`. Sensitive account, Provider-review, RBAC, and private-document access actions now append actor, target, reason, redacted before/after, request, and trace evidence to one immutable audit collection; database mutations and their audit writes share a transaction and audit reads require `admin:audit.view`. A Mongoose connection boundary, MongoDB readiness ping, environment-specific index policy, and local-only seed harness exist. Strict validation covers runtime environment, database and token configuration, request DTOs, and explicit response projections. Shared envelopes, the HTTP security baseline, validated request/trace context, and redacted structured completion logs are implemented. OpenAPI 3.1, Postman, and executable inventory agree on active HTTP routes. Account deletion/privacy policy under Q-012, password reset, downstream product modules, full React/web product surfaces, Production storage/scanner configuration, approved external provider deployments, and isolated live-test data remain absent. A partial React/Vite web foundation with SSR route, locale, and state shells plus deterministic tests now exists for `frontend_000`; it remains Partial and is not a completed product surface.
- Node.js `v22.18.0` and npm `11.6.4` are available; the repository declares Node `>=24 <25` and the planned environment baseline is Node 24 LTS.
- The Agent Pack contains an English screen registry and metadata for all 131 Screen IDs. The original Arabic handoff remains an external source identified by checksum in `09_sources/HANDOFF_REFERENCE.md`.
- Agent Pack audit is clean. Mutable task status and the selected task are authoritative only in `03_execution/TASK_STATE.json` and `step_info.json`; this snapshot does not duplicate them.
- Repository inventory: `docs/repository-inventory.md`.
- Visual pixel audit remains deferred until Figma/Drive frames are opened during frontend tasks.
- Shared localization primitives now define `ar`, `en`, and `zh-CN`, RTL/LTR direction metadata, strict partial localized content, deterministic truthful fallback, and a separate UI translation-key contract. The Admin location runtime now provides permission-gated, audited list/create/update/delete behavior for localized locations and neighborhoods with stable slugs, hierarchy validation, optional GeoJSON coordinates, ordering, activation, optimistic versions, search indexes, and deletion guards for child and Provider-application references. No production master data was seeded.
- Property categories and types now have localized, permission-gated Admin CRUD with category-to-type hierarchy, stable slugs, ordering, activation, optimistic concurrency, text/hierarchy indexes, reason-bearing audit, and guarded deletion for child or property references. No production taxonomy was fabricated.
- Features and services now use localized names, stable group keys and slugs, deterministic ordering, activation, optimistic versions, Admin view/manage permissions, text/group indexes, audit evidence, and guarded deletion for property/project references.
- Organizations now have provider-linked office/developer master-data schemas and backend-derived source identity rules; verified badges require genuine provider and organization approval. The published-only unauthenticated homepage read model is implemented at `/api/v1/public/home`, the bounded published-active property listing/search at `/api/v1/public/properties`, published property details with safe project/media/related projections at `/api/v1/public/properties/:slug`, and fixed-field comparison for one or two published-active properties at `/api/v1/public/properties/compare`; developer/company directory remains deferred to a later B4 task.
- CMS platform, contact, and social settings now use strict localized values, optional ordered URL-validated links, reasoned optimistic changes, append-only modification history, and published-only public eligibility; secret-like fields are rejected.
- About blocks and team members now have strict localized, ordered, versioned CMS models with deterministic sorting and published+active-only public projections; missing production content remains a safe empty state.
- Population CMS data now requires an explicit available/draft/unavailable state and sourced value metadata; unsourced or fabricated numbers are rejected. Real-estate tips are localized, ordered, and published+active-only in public projections.
- Provider projects now support verified-provider-owned localized CRUD plus reasoned optimistic submit/review/publish transitions. Administrative review requires `admin:projects.review`; reviewer identity, timestamps, and state changes are audit-backed. Public projection and revisions remain deferred to subsequent B3 tasks.
- Public developer/company directory and profile projections are implemented at `/api/v1/public/developers` and `/api/v1/public/developers/:slug`; only organizations and linked provider profiles with approved states are exposed, with published projects and published-active properties. Provider IDs, review metadata, and contact/storage internals remain private.
- Seeker saved properties are implemented with authenticated ownership, idempotent save/unsave behavior, a unique seeker/property boundary, and published-active-only projections at `/api/v1/seeker/favorites` and `/api/v1/seeker/favorites/:propertyId`.
- Seeker overview is implemented at `/api/v1/seeker/overview` as an authenticated, read-only aggregate of available request, viewing, favorite, and notification collections; unprovisioned or empty aggregates safely report zero rather than invented metrics.
- Seeker profile, locale account setting, and search-preference reads/updates remain self-owned at `/api/v1/me` and `/api/v1/me/preferences`; strict locale, range, unknown-field, and account-state validation is covered without adding an unsupported deletion/privacy route.
- Seeker notifications are implemented at `/api/v1/seeker/notifications` with bounded recipient-owned pagination, unread/type filters, localized safe projections, idempotent mark-read, and read-all counts; empty collections remain a truthful zero state.
- Public and seeker projection regression coverage verifies that internal notes, assignments, provider documents, audit records, credentials, storage keys, signed URLs, and access tokens are excluded while published/active and recipient ownership filters remain enforced.

Any code added later must rebuild this report from the actual repository rather than copying this snapshot.
