# GUIDE-02 local functional acceptance — 2026-09-12

GUIDE-02 covers property search, detail, comparison, developer context, and saving when the visitor has an active seeker account. `guide-runs/discovery-local-latest.json` passed 6/6 real AR/EN browser runs on Desktop, Tablet, and Pixel 5: live filters, offline retry, empty reset, two-property comparison, a published property detail, and developer directory-to-profile navigation all passed without page errors or horizontal overflow. Query validation and out-of-range pagination recovery are evidenced by the two dedicated discovery reports.

The authenticated save branch is covered by the real browser/API/MongoDB favorite evidence: `guide-04-local-latest.json` adds the property, proves idempotent repeat save, renders the saved projection, and removes it. `favorites-access-http-local-latest.json` proves ownership isolation, single-record duplicate save, provider/admin/anonymous denial, current-account state enforcement, invalid/unavailable-property rejection, and cleanup. `saved-remove-recovery-local-latest.json` covers offline removal recovery in all six locale/device combinations, and `favorites-logout-local-latest.json` proves logout invalidates read/add/remove operations.

Public discovery and comparison are reads; favorites are an idempotent seeker-owned set operation. Optimistic write versions, decision reasons, and audit rollback do not apply. GUIDE-02 remains globally `PARTIAL` until independent Figma acceptance, the final project-wide quality gate, and Production verification. Demo fixtures remain intact and no launch or purge ran.
