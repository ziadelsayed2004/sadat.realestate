# SEK-04 direct source reconciliation — 2026-09-13

The official Figma node `6027:7928` was read directly. Although the frame inventory names it “Contacted”, the visible canonical state is the scheduled viewing step. The runtime fixture now follows the source content: `REQ-4798`, viewing request, 3 August 2026 submission, current “Viewing” status, `Villa SDT-2103`, Corniche District, property code `SDT-2103`, Sadat Real Estate Development, EGP 2,500,000 maximum budget, and the approved weekend-morning note.

The detail route now assigns `SEK-04` to `scheduled`, uses the source labels “Viewing” and “Completed” in the lifecycle, and renders the assigned provider card separately from the linked property. AR/EN visual captures use the source-aligned 1551×863 viewport. The complete affected requests suite passed 12/12 across AR/EN after lint and the Web build passed.

The strict classification remains `PARTIAL_EXTERNAL`: the shared `/me` projection still lacks an approved avatar/media field, so the Figma identity image cannot be proven through the live account contract. Strict totals therefore remain 91/119, with 28 screens open.
