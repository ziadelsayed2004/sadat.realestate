# حالة اعتماد النسخة — 9 سبتمبر 2026

## تحديث ضمانات الطلبات — 9 سبتمبر

أُضيف تدقيق ذري لتحديثات المعاينات (تغيير الموعد والتأكيد والإلغاء والإكمال)، مع صاحب القرار والحالة والإصدار والموعد ومعرّفات التتبع. سبب إلغاء مقدم العقار يُحفظ كما أُرسل؛ إجراءات العميل التي لا تطلب سببًا في عقدها تسجل وصف العملية ولا تدّعي جمع سبب من العميل. فُرض حد الموعد المستقبلي نفسه عند الإنشاء وتغييره، والتحقق من المنطقة الزمنية، ومنع تغيير الموعد داخل إجراء التأكيد، ورفض تحويل null/false/string إلى expectedVersion رقمي. نجحت اختبارات المعاينات 11/11 وtypecheck وlint. دليل MongoDB الحقيقي يثبت التراجع عند فشل التدقيق، وتأكيدين لنفس المعاينة بنتيجة 200/409 وسجل واحد، وحفظ سبب الإلغاء. لا يثبت ذلك منع حجزين مستقلين لنفس العقار والموعد؛ هذه الفجوة ورحلة المعاينات عبر المتصفح ما زالتا مفتوحتين.

استُكمل مسار `GET /admin/viewings`: سياسة جلسة Admin و2FA المشتركة، وصلاحية `admin:viewings.view`، وفحص حالة الحساب الحالية. صلاحية العرض لا تتيح تنفيذ قرارات المعاينة عبر الخدمة. نجحت اختبارات المعاينات 9/9 وtypecheck وlint، وأُعيد اختبار منح الصلاحية وتعطيل الحساب عبر API وRBAC وMongoDB الحقيقيين في دليل `request-guarantees-local-latest.json`. هذا لا يغلق رحلة المعاينة: ما زالت مراجعة تضارب المواعيد وسجل قراراتها ودورتها الكاملة مطلوبة.

أُصلحت مسارات الطلبات الإدارية الستة لتطبيق سياسة مهلة جلسة Admin و2FA المشتركة، مع صلاحيات مستقلة: `admin:requests.view` و`manage` و`assign` و`notes`. يفشل الوصول الإداري إذا غاب مزود الصلاحيات، ويُعاد فحص حالة الحساب من MongoDB عند كل عملية. الإسناد يتطلب حساب Admin نشطًا للمستلم.

أصبح سبب انتقال الحالة الإداري إلزاميًا (3 أحرف على الأقل) في الخدمة والواجهة. انتقال الحالة والإسناد والملاحظة تكتب سجل التدقيق والتحديث داخل معاملة واحدة؛ السجل يتضمن صاحب العملية والسبب والإصدار السابق واللاحق ومعرّفات التتبع، دون نسخ نص الملاحظة الخاصة. يمنع فهرس فريد تقديمين متزامنين متطابقين للطلب الجديد؛ الفهرس يخص السجلات التي تنشئها النسخة الجديدة ولا يعيد كتابة السجلات القديمة.

الدليل [request-guarantees-local-latest.json](guide-runs/request-guarantees-local-latest.json) ناتج عن `node --import tsx scripts/verify-request-guarantees-local.mjs`: API حقيقي مع JWT موقّع وRBAC وMongoDB replica set في قاعدة محلية منفصلة. نجحت اختبارات منع الوصول لطلب مستخدم آخر، تعطيل الحساب مع بقاء التوكن صالحًا، سحب صلاحية الكتابة، رفض القرار بلا سبب، التراجع عن التحديث وسجل التدقيق عند فشل التدقيق، قرارين متزامنين بنتيجة واحدة ناجحة والأخرى 409، ومنع تكرار أثر الإسناد والملاحظة. لا يستخدم هذا الدليل استجابات HTTP محاكية؛ حقن فشل كاتب التدقيق مقصود لاختبار التراجع بعد إدخال السجل الحقيقي.

نجح `npm run quality` بالكامل: 585/585 اختبار API، وتغطية 81.03% lines و80.86% functions و77.84% branches وصفر ثغرات؛ سجل التشغيل `.local/logs/request-quality.log`. بعد إعادة بناء النسخة وتشغيلها نجحت رحلة التسجيل والحفظ والتواصل مرة أخرى. نُفذت قرارات المراجعة والتواصل والحل والإغلاق من واجهة Admin الحقيقية ثم ظهرت الحالة المغلقة في واجهة الباحث؛ الدليل [guide-04-local-latest.json](guide-runs/guide-04-local-latest.json).

هذه أدلة ضمانات للطلبات وليست إغلاقًا لرحلات GUIDE-06/21/26 كاملة. ما زال يلزم استكمال بقية أنواع الطلبات وقراراتها عبر واجهة الإدارة، ومراجعة بقية المسارات الإدارية ومنها المعاينات، وإعدادات الخصوصية، والمطابقة البصرية وProduction بعد نشر المستخدم.

هذا تحديث للتقرير السابق، وليس إعلانًا باكتمال المشروع أو اعتماد Production. الأرقام التالية تفصل بين سجلات الإغلاق البصري، الاختبارات المحلية، وتنفيذ الرحلات الحية.

## عدد الصفحات المعتمدة

أُعيد تشغيل `node scripts/audit-figma-screen-closure.mjs` في 9 سبتمبر، وكانت النتيجة:

| السطح | مغلق وفق سجل الاعتماد | ما زال مفتوحًا |
| --- | ---: | ---: |
| Admin | 64/66 | 2 |
| Auth | 19/19 | 0 |
| Provider | 7/24 | 17 |
| Seeker | 0/10 | 10 |
| الإجمالي داخل هذه البوابة | 90/119 | 29 |

هذه أعداد إغلاق السجل الحالي، وليست قياسًا جديدًا يثبت تطابق كل شاشة بنسبة 100% على Desktop وTablet وMobile. المخزون العام 131 شاشة؛ بوابة 119 أعلاه لا تشمل 12 شاشة Public. لا يجوز قسمة 90 على 131 أو اعتبار نجاح اختبارات Admin نسبة Figma.

سجلات القياس السابقة تحتوي 131/131 مرجع Desktop و62 عنصرًا في صفحة الـresponsive: 30 بعرض 402 و29 بعرض 1024 وثلاثة مراجع مكونات. وجود القياسات لا يعني اكتمال المقارنة بالتنفيذ. أُعيد استرجاع سياق تصميم الرئيسية Mobile من الملف الأصلي `0HBdTNGROmmpC6S7OYa3iJ` والعقدة `6017:110793`؛ عرضها 402 وارتفاعها 6287، بينما الإطار الأب ارتفاعه 6263. يجب مقارنة العنصر الصحيح وعدم استخدام قصّ الأب كارتفاع محتوى مفترض.

## ما أُنجز برمجيًا منذ التقرير السابق

| المجال | التنفيذ والأدلة المحلية | حد الاعتماد |
| --- | --- | --- |
| Property | إعدادات السياسة في `125b65d` و`65ee123`؛ إسقاط التواصل بحسب public/authenticated/after_request في `75df929` | يلزم استكمال الرحلة الحية وإعادة Production |
| Advertising | ربط الأنواع والمواضع والصيغ والأبعاد وصلاحية التسعير وطرق إثبات الدفع في `4208f3f` | الاختبارات المركزة ليست بديلًا لدورة حية كاملة |
| Community | سياسات الإشراف في `ed3d7dc`، بالإضافة إلى عقد النشر/الإخفاء والسبب والتدقيق وتعارض النسخة السابق | يلزم تكرار الدورة على النسخة المنشورة بعد النشر |
| Display/Population | ربط الإعدادات في `5c10761` وإصلاح ملف اللغة القديم في `0918571` | يلزم فحص سلوكها الحي ضمن GUIDE-25 |
| Admin authentication | مهلة الجلسة واشتراط OTP إضافي للإدارة في `1bbe5f9` | لا يغلق جميع إعدادات الخصوصية |
| Admin UI | 114 صورة baseline محدّثة و338/338 اختبار AR/EN سابق | visual regression محلي، وليس إثباتًا شاملًا لتطابق Figma |

في استئناف 9 سبتمبر أُصلح بقاء بيانات تواصل العقار عند تسجيل الخروج؛ تُزال الإسقاطات المرتبطة بالحساب عند تبديل الجلسة ويُحمّل إسقاط الزائر. أُضيف اختبار للانتقال نفسه.

الفحص الحقيقي للبحث على الموبايل كشف أن شريط تصنيفات العقارات كان يحتفظ بأعداد ونصوص نموذجية ويعرض إجماليًا ثابتًا `1,200+`. أصبح يستعمل أسماء وأعداد API، ويستبعد التصنيفات غير الموجودة. أُزيل الإجمالي الثابت لعدم وجود إجمالي عالمي موثوق في العقد الحالي. نجح اختبار القائمة والتفاصيل: **23/23**.

## فجوة Backend مؤكدة ما زالت مفتوحة

المفاتيح `hideCustomerContact` و`hideInternalNotes` و`hidePrivateDocuments` تُقرأ في `privacy-security-policy.ts`، لكن البحث في الكود لم يجد مستهلكين لها خارج قارئ الإعدادات. وجود قيود ملكية وصلاحيات حالية لا يثبت أن تغيير تلك المفاتيح يغيّر الإسقاط. يجب تحديد الإسقاط المصرح به لكل دور، توصيل الإعدادات، واختبار أثر الحفظ دون توسيع الاطلاع على البيانات الخاصة بصورة غير مقصودة.

## الرحلات من البداية للنهاية

المصفوفة الرسمية تشمل **26 رحلة / 131 ارتباط شاشة**. حالتها السابقة `MAPPED_PENDING_BACKEND_CYCLE_REVIEW`، ولا يصح نقلها إلى Complete تلقائيًا بعد نجاح حزمة الاختبارات.

الاختبار `apps/api/tests/testing/journeys.test.ts` يستخدم منفّذًا محاكيًا لجزء من الانتقالات، و`critical-journeys.spec.ts` يعترض استجابات API. كذلك `test:integration:live` يثبت جاهزية MongoDB وطوبولوجيتها فقط. هذه أدوات مفيدة، لكنها لا تثبت تشغيل الرحلات الـ26 مع قاعدة حقيقية.

بدأ الفحص الحي بانتقال البحث من الرئيسية إلى قائمة العقارات على عرض 402px. ثم نجح **GUIDE-04 محليًا** عبر المتصفح وSMTP وMongoDB الحقيقيين: اختيار الباحث، إرسال OTP بالبريد 202، التحقق 200، إنشاء حساب verified seeker برمز 201، ظهور شاشة النجاح، واستعادة الجلسة وفتح لوحة الباحث بعد تحميل كامل. أُثبت رفض وصول الباحث إلى إعدادات الإدارة 403، وتسجيل الخروج 200، ثم رفض refresh برمز 401. عرض الموبايل 402px بلا overflow. لا توجد استجابات API محاكية. الأدلة في `guide-runs/guide-04-local-latest.json` والسكريبت `scripts/verify-guide-registration-local.mjs`.

حُدثت المصفوفة إلى **3/26 ناجحة E2E محليًا (GUIDE-01 وGUIDE-02 وGUIDE-04)، 0/26 معتمدة على Production، 0/26 مغلقة نهائيًا**. ما زال يلزم التحقق السلبي من إعادة استخدام OTP وتكرار التسجيل. الدليل التاريخي يقول OTP للهاتف بينما السلطة الحالية بريد إلكتروني، وقد وُثّق الفرق صراحةً.

GUIDE-01/02: نجح البحث من الرئيسية، فلتر الإيجار، إعادة ضبط النتائج الفارغة دون document navigation، مقارنة عقارين، تفاصيل العقار، ودليل المطورين حتى الملف العام. اكتُشف غياب ربط زر الحفظ في التفاصيل وأُصلح؛ نجح بعده الحفظ من الزر، منع الحفظ المكرر، الانتقال للمحفوظات والإزالة من الواجهة ببيانات حقيقية. شُغلت الرحلتان بسكريبتَي `verify-guide-discovery-local.mjs` و`verify-guide-registration-local.mjs`. اختبارات التفاصيل الأخيرة **14/14**. موضع زر الحفظ الجديد يحتاج مراجعة بصرية قبل اعتماد Figma للصفحة، ولا يزيد عدّاد الإغلاق البصري تلقائيًا.

النشر حسب توجيه المستخدم الأخير ينفذه المستخدم بالطريقة المعتادة. يجب إبقاء seeded demo data لتجربة العميل، وعدم تشغيل وضع `empty` أثناء هذا التسليم.

### تحديث دورة التواصل — 9 سبتمبر، 07:55 UTC

الفحص الحي كشف عدم توافق حقيقيًا: واجهة التفاصيل ترسل `{type, payload}` إلى endpoint ينتظر حقول التواصل مباشرة، فيرفض الطلب بـ400. أُصلحت الواجهة لتبعث `request.payload` مع الحفاظ على التحقق بالعقد والتوثيق. نجحت اختبارات التفاصيل **14/14** والبناء وlint للملفات المعدّلة.

نجحت الدورة على API/MongoDB الحقيقيين: إنشاء الطلب من المتصفح 201، انتقال الإدارة عبر API إلى under_review ثم contacted ثم resolved ثم closed، رفض محاولة الباحث تنفيذ قرار إداري 403، رفض expectedVersion قديم 409، ثم عرض Closed للباحث في المتصفح. الدليل `contactJourneyEvidence` داخل `guide-runs/guide-04-local-latest.json`؛ السكريبت `verify-guide-contact-local.mjs`.

سُجلت هذه كدورة تواصل ناجحة ضمن **GUIDE-06 وGUIDE-21 جزئيًا**، وليست إغلاقًا لكل طلبات الدليل: قرارات الإدارة من الواجهة وبقية أنواع الطلبات وProduction ما زالت مطلوبة. الإجمالي يبقى 3 رحلات محلية كاملة النطاق المسجل، ورحلتين جزئيتين، وصفر اعتماد نهائي على Production.

مراجعة الكود أظهرت أيضًا أن خدمة الطلبات تتحقق من دور Admin دون تصريح تفصيلي داخل الخدمة، ومستودع انتقالاتها لا يكتب سبب القرار أو سجل تدقيق. لا تُحسب اختبارات الانتقال أعلاه دليلًا على تدقيق الطلبات أو اكتمال RBAC الخاص بها؛ يلزم استكمال هذه الضمانات ضمن عمل الباك إند المتبقي.

## الجودة والأمان

آخر حزمة API مكتملة أثناء هذا الاستئناف: **582/582**؛ coverage **80.98% lines، 77.79% branches، 80.69% functions**، أعلى من الحدود 80/70/80.

فحص الجودة كشف ثغرات اعتماديات حالية، لذلك حُدث Nodemailer من 9.0.5 إلى 9.1.1 وVitest من 4.1.10 إلى 4.1.11، مع تثبيت النسخ في سياسة الـworkspace وملف القفل. اكتمل `npm run quality` بعد التحديث **بنجاح وخروج 0**، شاملًا lint وtypecheck واختبارات workspace وتغطية API والبناء والعقود وOpenAPI وPostman وتدقيق الاعتماديات وسجل المشروع. تدقيق الأمان: **صفر ثغرات**. السجل المحلي `.local/logs/root-cleanup-2026-09-09/.codex-quality-6.log`.

مراجع الإصلاح: [Nodemailer 9.1.1](https://github.com/nodemailer/nodemailer/releases/tag/v9.1.1)، [Nodemailer 9.1.0](https://github.com/nodemailer/nodemailer/releases/tag/v9.1.0)، [Vitest advisory](https://github.com/advisories/GHSA-82fw-gwwq-j7x9).

## ما لم يُقفل بصريًا أو على Production

- `ADM-18`: تعارض مصدر السايدبار واتجاهه، مع فرق مادي سابق 39.7325%؛ يحتاج حسم المرجع والمقارنة مجددًا.
- `ADM-54`: المرجع الحالي PNG محلي owner-authored؛ أُزيل مرجع HTML مفقود من سجلين، ونجح `audit_pack` دون أخطاء. هذا يصحح السجل ولا يسترجع Figma ولا يغلق المطابقة.
- 17 Provider و10 Seeker ما زالت خارج الإغلاق الصارم؛ يلزم المقارنة على المقاسات المطلوبة.
- لم يُنشر تغيير جديد أثناء هذا الاستئناف. نتيجة تسجيل Admin وAPI العقارات 200 في الفحص السابق لا تعتمد آخر الكود.
- فحص Production للقراءة فقط في 9 سبتمبر أكد الرئيسية **200** وAPI العقارات **200** والمقالات **200**، لكن `/api/v1/public/settings/seo` ما زال **404**. الأدلة: `production-readonly-2026-09-09.json`. لا يُعد هذا تنفيذًا للرحلات أو إثبات نشر الإصلاحات.
- يلزم نشر آخر كود عبر قناة Hostinger صالحة، ثم 102 زيارة Admin وPixel 5 بعرض 393px وفحص property/article/SEO APIs ودورتي community/advertising على Production.

## نقطة الاستئناف

الجودة بعد ترقيات الأمان ناجحة. التالي تشغيل الرحلات الحية وتحديث المصفوفة بأدلة لكل انتقال وصلاحية وتعارض. تُستكمل إسقاطات الخصوصية والقياسات البصرية، ثم النشر والفحص النهائي. الجول المسجل بحالة `usageLimited` وليس Complete؛ العمل الحالي لا يغيّر حد استخدامه تلقائيًا.
