# Article Categories

Article categories use stable slugs, localized names, bounded display ordering, and explicit active flags. Category mutations are administrative and reject duplicate slugs; localized CMS values remain separate from interface translation keys.
