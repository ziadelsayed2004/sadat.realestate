# Public Community Projection

The public feed contains published posts only and includes visible comments with truthful counts through `GET /api/v1/public/community/posts` and `GET /api/v1/public/community/posts/:postId`. Hidden or removed posts and comments are excluded; administrative moderation fields, reporter identity, author identity, and internal state are not projected.
