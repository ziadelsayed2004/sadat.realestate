# Viewings API

Viewing creation records a request for a future property time using the supplied IANA timezone. The platform does not claim a guaranteed slot without an approved availability model; providers confirm or reschedule explicitly. Seeker edits and cancellation are ownership-scoped and require the current optimistic version. Provider transitions are explicit and reject stale or invalid state changes.
