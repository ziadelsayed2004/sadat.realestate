# Public Articles

Public article listing and slug details expose only `published` articles. Requested locales support deterministic fallback to Arabic, then English, then Simplified Chinese, while SEO metadata follows the same safe localized projection. Draft, review, and archived articles never leak into public reads.
