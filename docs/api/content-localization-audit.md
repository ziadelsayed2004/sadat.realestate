# Content and Localization Audit

The quality audit validates supported content locales (`ar`, `en`, `zh-CN`), reports missing translations without inventing content, applies deterministic fallback, and exposes the established direction metadata (`ar` RTL; English and Simplified Chinese LTR). Localized CMS values remain separate from interface translation keys.
