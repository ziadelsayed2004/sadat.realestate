[Socket]
# Replace Ubuntu's default Unix-only activation socket. The application uses a
# loopback TCP adapter so ClamAV remains private while readiness and scanning use
# the same endpoint.
ListenStream=
ListenStream=127.0.0.1:3310
SocketUser=clamav
SocketGroup=clamav

