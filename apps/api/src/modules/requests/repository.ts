import { Types, type Connection } from 'mongoose';
import type { RequestListQuery } from '@sadat-real-estate/contracts';
import type { RequestRecord, RequestRepository } from './service.js';

function toObjectId(id: string): Types.ObjectId { return new Types.ObjectId(id); }
function row(value: Record<string, unknown>): RequestRecord | undefined { const id = value._id instanceof Types.ObjectId ? value._id.toHexString() : typeof value._id === 'string' ? value._id : undefined; if (!id || !(value.createdAt instanceof Date) || !(value.updatedAt instanceof Date) || typeof value.type !== 'string' || typeof value.status !== 'string' || typeof value.source !== 'string' || typeof value.version !== 'number') return undefined; return { id, type: value.type as RequestRecord['type'], source: value.source as RequestRecord['source'], ...(typeof value.creatorId === 'string' ? { creatorId: value.creatorId } : value.creatorId instanceof Types.ObjectId ? { creatorId: value.creatorId.toHexString() } : {}), ...(value.seekerId instanceof Types.ObjectId ? { seekerId: value.seekerId.toHexString() } : {}), ...(value.providerId instanceof Types.ObjectId ? { providerId: value.providerId.toHexString() } : {}), ...(value.propertyId instanceof Types.ObjectId ? { propertyId: value.propertyId.toHexString() } : {}), ...(value.projectId instanceof Types.ObjectId ? { projectId: value.projectId.toHexString() } : {}), status: value.status as RequestRecord['status'], payload: (value.payload ?? {}) as Record<string, unknown>, ...(value.assignedTo instanceof Types.ObjectId ? { assignedTo: value.assignedTo.toHexString() } : {}), ...(Array.isArray(value.internalNotes) ? { internalNotes: value.internalNotes as NonNullable<RequestRecord['internalNotes']> } : {}), ...(value.dueAt instanceof Date ? { dueAt: value.dueAt } : {}), ...(value.appointmentAt instanceof Date ? { appointmentAt: value.appointmentAt } : {}), ...(typeof value.appointmentTimezone === 'string' ? { appointmentTimezone: value.appointmentTimezone } : {}), version: value.version, createdAt: value.createdAt, updatedAt: value.updatedAt } as RequestRecord; }
export function createMongooseRequestRepository(connection: Connection): RequestRepository { const requests = connection.collection('requests'); return { async create(request) { try { const doc = { _id: toObjectId(request.id), type: request.type, source: request.source, ...(request.creatorId ? { creatorId: toObjectId(request.creatorId) } : {}), ...(request.seekerId ? { seekerId: toObjectId(request.seekerId) } : {}), ...(request.providerId ? { providerId: toObjectId(request.providerId) } : {}), ...(request.propertyId ? { propertyId: toObjectId(request.propertyId) } : {}), ...(request.projectId ? { projectId: toObjectId(request.projectId) } : {}), status: request.status, payload: request.payload, ...(request.dueAt ? { dueAt: request.dueAt } : {}), version: request.version, createdAt: request.createdAt, updatedAt: request.updatedAt }; await requests.insertOne(doc); return { kind: 'written', request }; } catch (error) { if (typeof error === 'object' && error !== null && 'code' in error && (error as { code?: number }).code === 11000) return { kind: 'duplicate' }; throw error; } }, async list(query: RequestListQuery, scope) { const filter: Record<string, unknown> = {}; if (query.status) filter.status = query.status; if (query.type) filter.type = query.type; if (query.source) filter.source = query.source; if (scope?.seekerId) filter.seekerId = toObjectId(scope.seekerId); if (scope?.providerId) filter.providerId = toObjectId(scope.providerId); const [rows, total] = await Promise.all([requests.find(filter).sort({ createdAt: -1, _id: -1 }).skip((query.page - 1) * query.limit).limit(query.limit).toArray(), requests.countDocuments(filter)]); return { items: rows.flatMap(value => { const parsed = row(value as Record<string, unknown>); return parsed ? [parsed] : []; }), total }; }, async get(id, scope) { const filter: Record<string, unknown> = { _id: toObjectId(id) }; if (scope?.seekerId) filter.seekerId = toObjectId(scope.seekerId); if (scope?.providerId) filter.providerId = toObjectId(scope.providerId); const value = await requests.findOne(filter); return value ? row(value as Record<string, unknown>) : undefined; }, async transition(input) { const result = await requests.findOneAndUpdate({ _id: toObjectId(input.id), version: input.expectedVersion }, { $set: { status: input.status, updatedAt: input.now }, $inc: { version: 1 } }, { returnDocument: 'after' }); if (!result) return { kind: 'version_conflict' }; const parsed = row(result as unknown as Record<string, unknown>); return parsed ? { kind: 'written', request: parsed } : { kind: 'not_found' }; }, async assign(input) { const result = await requests.findOneAndUpdate({ _id: toObjectId(input.id), version: input.expectedVersion }, { $set: { assignedTo: toObjectId(input.assigneeId), updatedAt: input.now }, $inc: { version: 1 } }, { returnDocument: 'after' }); if (!result) return { kind: 'version_conflict' }; const parsed = row(result as unknown as Record<string, unknown>); return parsed ? { kind: 'written', request: parsed } : { kind: 'not_found' }; }, async addNote(input) { const result = await requests.findOneAndUpdate({ _id: toObjectId(input.id), version: input.expectedVersion }, ({ $push: { internalNotes: input.note }, $set: { updatedAt: input.now }, $inc: { version: 1 } } as never), { returnDocument: 'after' }); if (!result) return { kind: 'version_conflict' }; const parsed = row(result as unknown as Record<string, unknown>); return parsed ? { kind: 'written', request: parsed } : { kind: 'not_found' }; } }; }
