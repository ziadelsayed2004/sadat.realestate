import { Types, type Connection } from 'mongoose';
import type { ViewingListQuery } from '@sadat-real-estate/contracts';
import type { ViewingRecord, ViewingRepository } from './service.js';
function oid(value: string): Types.ObjectId { return new Types.ObjectId(value); }
function parse(value: Record<string, unknown>): ViewingRecord | undefined { const id = value._id instanceof Types.ObjectId ? value._id.toHexString() : undefined; if (!id || !(value.requestedAt instanceof Date) || !(value.createdAt instanceof Date) || !(value.updatedAt instanceof Date) || typeof value.propertyId !== 'string' || !(value.seekerId instanceof Types.ObjectId) || typeof value.status !== 'string' || typeof value.timezone !== 'string' || typeof value.version !== 'number') return undefined; return { id, propertyId: value.propertyId, seekerId: value.seekerId.toHexString(), ...(value.providerId instanceof Types.ObjectId ? { providerId: value.providerId.toHexString() } : {}), status: value.status as ViewingRecord['status'], requestedAt: value.requestedAt, timezone: value.timezone, ...(typeof value.note === 'string' ? { note: value.note } : {}), version: value.version, createdAt: value.createdAt, updatedAt: value.updatedAt }; }
export function createMongooseViewingRepository(connection: Connection): ViewingRepository { const collection = connection.collection('viewings'); return { async create(row) { await collection.createIndex({ propertyId: 1, requestedAt: 1, status: 1 }); await collection.insertOne({ _id: oid(row.id), propertyId: row.propertyId, seekerId: oid(row.seekerId), ...(row.providerId ? { providerId: oid(row.providerId) } : {}), status: row.status, requestedAt: row.requestedAt, timezone: row.timezone, ...(row.note ? { note: row.note } : {}), version: row.version, createdAt: row.createdAt, updatedAt: row.updatedAt }); return row; }, async list(query: ViewingListQuery, scope) { const filter: Record<string, unknown> = {}; if (query.status) filter.status = query.status; if (scope.seekerId) filter.seekerId = oid(scope.seekerId); if (scope.providerId) filter.providerId = oid(scope.providerId); const [rows, total] = await Promise.all([collection.find(filter).sort({ requestedAt: 1, _id: 1 }).skip((query.page - 1) * query.limit).limit(query.limit).toArray(), collection.countDocuments(filter)]); return { items: rows.flatMap(value => { const parsed = parse(value as Record<string, unknown>); return parsed ? [parsed] : []; }), total }; }, async get(id, scope) { const filter: Record<string, unknown> = { _id: oid(id) }; if (scope.seekerId) filter.seekerId = oid(scope.seekerId); if (scope.providerId) filter.providerId = oid(scope.providerId); const value = await collection.findOne(filter); return value ? parse(value as Record<string, unknown>) : undefined; }, async update(input) { const result = await collection.findOneAndUpdate({ _id: oid(input.id), version: input.expectedVersion }, { $set: { ...(input.status ? { status: input.status } : {}), ...(input.requestedAt ? { requestedAt: input.requestedAt } : {}), ...(input.timezone ? { timezone: input.timezone } : {}), updatedAt: input.now }, $inc: { version: 1 } }, { returnDocument: 'after' }); if (!result) return { kind: 'version_conflict' }; const row = parse(result as unknown as Record<string, unknown>); return row ? { kind: 'written', row } : { kind: 'not_found' }; } }; }
