import assert from 'node:assert/strict'; import test from 'node:test'; import type { AccessTokenClaims } from '../../src/modules/auth/crypto.js'; import { createCommunityReportService } from '../../src/modules/moderation/community-reports.js';
const seeker = { iss: 'sadat-realestate-api', aud: 'sadat-realestate', sub: '0123456789abcdef01234567', sid: '1123456789abcdef01234567', role: 'seeker', status: 'verified', iat: 1, exp: 9999999999, jti: 'test' } as AccessTokenClaims; const admin = { ...seeker, role: 'admin' } as AccessTokenClaims;
test('community reports support bounded abuse reporting and reasoned admin actions', async () => { const service = createCommunityReportService(); const report = await service.create(seeker, { postId: '4123456789abcdef01234567', reason: 'spam', details: 'Repeated promotional content' }); assert.equal((await service.list(admin)).length, 1); const resolved = await service.resolve(admin, report.id, 'resolve', 'Removed promotional post', 0); assert.equal(resolved.status, 'resolved'); await assert.rejects(() => service.resolve(seeker, report.id, 'dismiss', 'x', 1), /FORBIDDEN/); });
