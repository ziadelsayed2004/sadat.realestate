import fs from 'node:fs';
import { chromium } from '@playwright/test';
const b=await chromium.launch({headless:true});const p=await b.newPage({viewport:{width:4000,height:1000}});
await p.setContent(`<img id="i" src="data:image/png;base64,${fs.readFileSync('docs/quality/figma_parity/screens/PUB-03/figma-high-provider.png').toString('base64')}">`);
const result=await p.evaluate(()=>{const im=document.querySelector('#i'),c=document.createElement('canvas');c.width=im.naturalWidth;c.height=im.naturalHeight;const x=c.getContext('2d');x.drawImage(im,0,0);const d=x.getImageData(0,0,c.width,c.height).data;const groups=(x0,y0,w,h,pred)=>{const rows=[];for(let y=y0;y<y0+h;y++){let n=0,min=c.width,max=-1;for(let x=x0;x<x0+w;x++){const i=(y*c.width+x)*4;if(pred(d[i],d[i+1],d[i+2])){n++;min=Math.min(min,x);max=Math.max(max,x)}}if(n)rows.push({y,min,max,n})}const gs=[];for(const r of rows){const g=gs.at(-1);if(!g||r.y>g.at(-1).y+1)gs.push([r]);else g.push(r)}return gs.map(g=>({y:g[0].y,height:g.at(-1).y-g[0].y+1,min:Math.min(...g.map(r=>r.min)),max:Math.max(...g.map(r=>r.max)),width:Math.max(...g.map(r=>r.max))-Math.min(...g.map(r=>r.min))+1,pixels:g.reduce((s,r)=>s+r.n,0)}))};const dark=(r,g,b)=>r<180&&g<185&&b<200&&Math.max(r,g,b)-Math.min(r,g,b)<80;return {dark:groups(1000,100,2200,550,dark)};});console.log(JSON.stringify(result,null,2));await b.close();
